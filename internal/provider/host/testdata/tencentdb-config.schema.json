{
  "type": "object",
  "additionalProperties": false,
  "required": ["base_url", "service_id", "timeout_ms"],
  "properties": {
    "base_url": {"type": "string"},
    "service_id": {"type": "string"},
    "timeout_ms": {"type": "integer", "minimum": 100, "maximum": 30000}
  }
}
